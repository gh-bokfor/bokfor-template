name: Bokföring

on:
  pull_request:
    branches:
      - main

jobs:
  verifiera:
    name: Verifiera bokföring
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v2
      - name: Setup
        uses: gh-bokfor/init@main
      - name: Verifiering
        uses: gh-bokfor/verifiera@main
